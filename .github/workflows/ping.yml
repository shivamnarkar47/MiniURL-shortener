name: Ping MiniURL
on:
  schedule:
    - cron: '*/40 * * * * *' # Every 40 seconds
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping service
        run: curl -X GET https://miniurl-shortener-1.onrender.com
